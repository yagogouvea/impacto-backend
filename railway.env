# Railway Environment Variables
# IMPORTANTE: DATABASE_URL será configurada automaticamente pelo Railway
# Não configure manualmente - use a interface do Railway

NODE_ENV=production
PORT=8080
JWT_SECRET=railway-jwt-secret-key-change-in-production-environment
BASE_URL=https://api.costaecamargo.seg.br
FRONTEND_URL=https://painel.costaecamargo.seg.br

# Configurações específicas para produção
CORS_ORIGINS=https://painel.costaecamargo.seg.br,https://cliente.painelsegtrack.com.br,https://app.painelsegtrack.com.br

# Configurações de segurança para produção
HELMET_ENABLED=true
COMPRESSION_ENABLED=true
RATE_LIMIT_ENABLED=true

# Configurações de log para produção
LOG_LEVEL=info
LOG_REQUESTS=true

# Para configurar DATABASE_URL:
# 1. Crie um serviço PostgreSQL no Railway
# 2. A variável será criada automaticamente
# 3. Copie o valor para o serviço do backend
